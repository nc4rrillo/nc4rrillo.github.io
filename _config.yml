# Permalinks
permalink:           pretty

# Setup
title:               Indigo
tagline:             Indigo wants to be a UI framework when it grows up.
paginate:            1
baseurl:             ""
# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _sass
  style:            :compressed
  
# Gems
gems:
  - jekyll-paginate
  - jekyll-gist
